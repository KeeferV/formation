<html>
<head>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="form.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <% if (user) { %>
  <a href="getUserProducts">Show orders for this user</a>
  <% } %>
  <div>
    <h3>All Products</h3>
    <ul>
  <% products.forEach(function(product){ %>
  <!--<li><a onclick="order('<%=product._id%>')" href="#" xhref="order/<%=product.id%>"><%=product.name%></a> - €<%=product.EUR_price%></li>-->
  <li><a href="order/<%=product.id%>">Order</a> <%=product.name%> - €<%=product.EUR_price%> (ordered <%=product.orders_counter%> times)</li>
  <% }); %>
  </ul>
  </div>
</body>
</html>